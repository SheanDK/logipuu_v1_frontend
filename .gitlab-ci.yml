image: node:20

before_script:
  - apt-get update
  - apt-get install -y python3 make g++ libavahi-compat-libdnssd-dev
  - npm ci

build:
  script:
    - npm run build
  artifacts:
    paths:
      - build/